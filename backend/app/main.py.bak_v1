from fastapi import FastAPI

app = FastAPI(
    title="IA Trabalhista Robusta",
    version="0.1.0",
)

@app.get("/health", tags=["ops"])
def health():
    return {"ok": True, "service": "ia_trabalhista_robusta", "version": "0.1.0"}
